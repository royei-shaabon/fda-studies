Day 6: Tool Use — What Actually Makes Something an "Agent"

This is the mechanic that turns a chat wrapper into something that can actually act on real data and do real computation reliably — the model decides what needs to happen, your code does the actual work. This is the direct line to your take-home's deterministic scoring requirement.

Day 6 syllabus:

Import/export gap closure — brief, mechanics only
Why tools exist — chatbot vs. agent, what the LLM decides vs. what code executes
Tool schemas — how tools are described to the model
The tool-call loop — request → execute → respond
Deterministic logic belongs in tools — tied to Day 3's scoring exercise
Capstone — minimal agent with two tools, end-to-end