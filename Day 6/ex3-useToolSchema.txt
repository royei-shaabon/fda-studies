Exercise: Write a tool schema (this exact shape) for a tool called lookup_product, which takes a productId (string) and returns product details from a hardcoded dataset. Write the name, a description that would actually help the model decide when to use it correctly (versus, say, guessing product details itself), and the input_schema with a well-described productId parameter. Think about what could go wrong with a vague productId description, given what a model might generate for it (e.g., could it hallucinate an ID that looks plausible but doesn't exist?).